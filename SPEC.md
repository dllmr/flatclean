# Requirements

We need to create a single-file Python script to identify folders under ~/.var/app that were created by Flatpaks that have since been removed from the system.

The script should have a PEP 723 dependency block such that it can be run with uv. Minimum Python version 3.12.

Ask if you have questions, or if the spec seems flawed.